{
  "name": "dla",
  "version": "1.0.1",
  "description": "node.js data loader with caching and support of lists",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist coverage",
    "build": "tsc",
    "test": "jest",
    "lint": "tslint \"src/**/*\"",
    "prepublishOnly": "npm run lint && npm run clean && npm run build && npm test"
  },
  "keywords": [
    "data",
    "loader"
  ],
  "author": "Roman Ditchuk",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^23.1.1",
    "@types/node": "^10.3.5",
    "jest": "^23.1.0",
    "source-map-support": "^0.5.6",
    "ts-jest": "^22.4.6",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  },
  "dependencies": {
    "object-hash": "^1.3.0"
  }
}
